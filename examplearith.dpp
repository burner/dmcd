module hello.world.gggg; 

static import bar;

import std.stdio : rename, foo = iii;

enum ENUM {
	integer,
	foobar,
	argsbar
}

enum ENUM {
	integer,
	foobar,
	argsbar
}

import std.stdio : rename, foo = iii;

enum ENUM {
	integer,
	foobar,
	argsbar
}

enum ENUM {
	integer,
	foobar,
	argsbar = assign
}
